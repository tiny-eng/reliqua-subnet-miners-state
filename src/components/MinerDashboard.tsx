// Kept as a thin alias for backward compat — single-hotkey embeds keep working.
// Real per-hotkey rendering lives in MinerPanel; multi-hotkey orchestration
// lives in MultiMinerDashboard.
export { default } from './MinerPanel'
